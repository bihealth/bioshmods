## TODO

### Package metadata and docs

* [x] Replace placeholder DESCRIPTION fields:
  * [x] `Title: What the Package Does (One Line, Title Case)`
  * [x] `Description: What the package does (one paragraph).`
* [x] Replace template README text with real package overview and example usage.
* [x] Verify install instructions in README (CRAN vs GitHub dev install).

### R CMD check notes

* [x] Remove unused import `ggrepel` from DESCRIPTION.
* [x] Resolve namespace notes for undefined globals/functions by fully qualifying calls:
  * [x] `setNames`
  * [x] `head`
  * [x] `str`
  * [x] `var`

### Tests

* [ ] Expand test coverage beyond the current `geneBrowserTable` smoke test.
* [ ] Add tests for new input-normalization/validation helpers:
  * [ ] disco normalization (`.normalize_disco_inputs`)
  * [ ] volcano normalization (`.normalize_volcano_inputs`)
  * [ ] gene-group selector normalization helpers
  * [ ] file export object normalization helpers

### Repo hygiene

* [x] Add/update `.gitignore` to exclude local editor/runtime artifacts (`*.swp`, `.Rhistory`, `*.Rcheck`, `*.tar.gz` build outputs).
* [x] Remove existing local swap/check/build artifacts from working tree.

### Known functional issues (existing)

* [x] `nearPoints()` issue in disco plot selection logic is resolved.
* [ ] Gene plot UI height does not adjust when resolution is changed.
