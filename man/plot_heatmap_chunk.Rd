% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{plot_heatmap_chunk}
\alias{plot_heatmap_chunk}
\title{Generate code for an expression heatmap}
\usage{
plot_heatmap_chunk(
  exprs,
  genes,
  covar = NULL,
  sample_id_col = "SampleID",
  annot = NULL,
  primary_id_col = "PrimaryID",
  annot_row_col = NULL,
  sel_annot = NULL,
  sort_by_covar = FALSE,
  legend = TRUE,
  palettes = NULL,
  col = NULL,
  env_map = NULL
)
}
\arguments{
\item{exprs}{Numeric matrix/data frame with genes in rows and samples in columns.
Row names must contain gene identifiers.}

\item{genes}{Character vector with selected gene identifiers.}

\item{covar}{Optional sample covariate data frame. Sample identifiers must be in
the column designated by \code{sample_id_col}.}

\item{sample_id_col}{Name of the sample ID column in \code{covar}.}

\item{annot}{Optional annotation data frame for genes.}

\item{primary_id_col}{Column in \code{annot} containing gene identifiers matching
\code{exprs} row names.}

\item{annot_row_col}{Optional column in \code{annot} used as row labels in the heatmap.
If \code{NULL} or missing in \code{annot}, row names from \code{exprs} are shown.}

\item{sel_annot}{Character vector of column names from \code{covar} to display as
top annotations. If \code{NULL} (default), no annotation bars are shown.}

\item{sort_by_covar}{Logical; if \code{TRUE} and covariates are selected in
\code{sel_annot}, samples are ordered by those covariates and column clustering
is disabled.}

\item{legend}{Logical; whether heatmap and annotation legends should be shown.}

\item{palettes}{Optional named list of palette definitions. When \code{col} is
\code{NULL} and \code{palettes$values$pal} exists, that value is used for heatmap
colors.}

\item{col}{Optional heatmap color mapping passed to
\code{ComplexHeatmap::Heatmap(col=...)}. This overrides \code{palettes}.}

\item{env_map}{optional list mapping \code{exprs}, \code{genes}, \code{covar}, \code{annot},
\code{palettes}, and \code{col} to names used in the generated code.}
}
\value{
a list with metadata and generated code
}
\description{
Generate readable R code that mirrors \code{\link[=plot_heatmap]{plot_heatmap()}} without calling it.
}
