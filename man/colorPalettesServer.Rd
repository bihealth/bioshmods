% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_color_palettes.R
\name{colorPalettesUI}
\alias{colorPalettesUI}
\alias{colorPalettesServer}
\title{Color Palettes Module UI}
\usage{
colorPalettesUI(id)

colorPalettesServer(id, variables, palettes = NULL, compact = FALSE)
}
\arguments{
\item{id}{Shiny module id (same as passed to \code{\link[=colorPalettesUI]{colorPalettesUI()}}).}

\item{variables}{Variable specification input as either a named list or a
reactive value/expression returning one. Single-dataset input can be a
variable specification list or a covariate data frame. Multi-dataset
input must be a named list where each element is a variable specification
list or covariate data frame.}

\item{palettes}{Optional reactiveVal-like placeholder kept for compatibility.
If supplied, it must be a function; the module returns its own reactive
palette specification and does not mutate external state. The value is a
named list with one element per dataset.}

\item{compact}{Logical; whether to use a more compact layout for palette rows.}
}
\value{
A UI container for dynamic variable-specific palette controls.

The reactive expression returning the current palette list (same
as the \code{palettes} argument if supplied).
}
\description{
Create palette controls for each declared variable and return the selected
palettes as a reactive expression.
}
\details{
A "variable specification list" is a named list where each element describes
one variable. Each variable entry must include:
\itemize{
\item \code{type}: one of \code{"categorical"}, \code{"ordinal"}, or \code{"continuous"}.
\item for \code{"categorical"} / \code{"ordinal"}: \code{levels} (non-empty character vector).
\item for \code{"continuous"}: \code{breaks} (numeric vector with at least 2 unique values).
}

Single-dataset form:

\if{html}{\out{<div class="sourceCode">}}\preformatted{list(sex=list(type="categorical", levels=c("female", "male")), 
     expression=list(type="continuous", breaks=c(-2, 0, 2)))
}\if{html}{\out{</div>}}

Multi-dataset form:

\if{html}{\out{<div class="sourceCode">}}\preformatted{list(ds_a=list(sex=list(type="categorical", 
                         levels=c("female", "male"))), 
      ds_b=list(expression=list(type="continuous", 
                                breaks=c(-2, 0, 2))))
}\if{html}{\out{</div>}}
}
\examples{
if(interactive()) {
  variables <- list(
    sex=list(type="categorical", levels=c("female", "male")),
    stage=list(type="ordinal", levels=c("I", "II", "III", "IV")),
    expression=list(type="continuous", breaks=c(-2, -1, 0, 1, 2))
  )

  ui <- fluidPage(
    colorPalettesUI("pal"),
    h4("Current palette keys"),
    verbatimTextOutput("pal_keys")
  )

  server <- function(input, output, session) {
    vars <- reactiveVal(variables)
    palettes <- colorPalettesServer("pal", variables=vars)

    output$pal_keys <- renderPrint(names(palettes()))
  }

  shinyApp(ui, server)
}

}
