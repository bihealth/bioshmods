% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_heatmap.R
\name{heatmapUI}
\alias{heatmapUI}
\alias{heatmapServer}
\title{Heatmap Module UI}
\usage{
heatmapUI(id)

heatmapServer(
  id,
  annot,
  exprs = NULL,
  cntr = NULL,
  covar = NULL,
  palettes = NULL,
  selection = NULL,
  sample_id_col = "SampleID",
  primary_id = "PrimaryID",
  dge_pval_col = "pvalue",
  dge_lfc_col = "log2FoldChange",
  dge_fdr_col = "padj",
  max_genes = 150,
  annot_row_col = NULL
)
}
\arguments{
\item{id}{Module identifier (same as passed to \code{\link[=heatmapUI]{heatmapUI()}}).}

\item{annot}{Named list of dataset annotation data frames, as in
\code{\link[=geneGroupSelectorServer]{geneGroupSelectorServer()}}. For a single dataset, use \code{list(default=annot)}.}

\item{exprs}{Named list of dataset expression matrices/data frames, as in
\code{\link[=geneGroupSelectorServer]{geneGroupSelectorServer()}}. For a single dataset, use \code{list(default=exprs)}.}

\item{cntr}{Optional contrast object, same format as \code{\link[=geneGroupSelectorServer]{geneGroupSelectorServer()}}.}

\item{covar}{Named list of dataset sample covariate data frames. For a single
dataset, use \code{list(default=covar)}.}

\item{palettes}{Optional reactive expression or \code{reactiveVal} returning a
palette list forwarded to \code{\link[=plot_heatmap]{plot_heatmap()}}. Useful when heatmap colors are
coordinated across modules.}

\item{selection}{Optional incoming \code{reactiveValues()} object with an \code{ids}
element containing gene IDs to import into the embedded gene-group
selector. Heatmap selections are not written back there.}

\item{sample_id_col}{Name of the sample ID column in \code{covar}.}

\item{primary_id}{Primary identifier column in \code{annot} / \code{cntr}, also used to
map selected genes to row labels in \code{\link[=plot_heatmap]{plot_heatmap()}}.}

\item{dge_pval_col}{Optional p-value column name for DGE mode in
\code{\link[=geneGroupSelectorServer]{geneGroupSelectorServer()}}.}

\item{dge_lfc_col}{Optional log fold-change column name for DGE mode in
\code{\link[=geneGroupSelectorServer]{geneGroupSelectorServer()}}.}

\item{dge_fdr_col}{Optional adjusted p-value (FDR) column name for DGE mode in
\code{\link[=geneGroupSelectorServer]{geneGroupSelectorServer()}}.}

\item{max_genes}{Hard limit for the number of genes shown on the heatmap.
If more genes are selected, only the first \code{max_genes} are displayed.}

\item{annot_row_col}{Optional default annotation column used for heatmap row labels.
Can be changed interactively in the module UI.}
}
\value{
A list with reactives: \code{genes}, \code{dataset}, and \code{heatmap}.
}
\description{
This module embeds \code{\link[=geneGroupSelectorUI]{geneGroupSelectorUI()}} / \code{\link[=geneGroupSelectorServer]{geneGroupSelectorServer()}}
on the left side and renders a heatmap for selected genes on the right side.
}
\examples{
annot <- data.frame(
  PrimaryID=paste0("g", 1:6),
  SYMBOL=LETTERS[1:6],
  stringsAsFactors=FALSE
)
exprs <- matrix(
  rnorm(24),
  nrow=6,
  dimnames=list(annot$PrimaryID, paste0("s", 1:4))
)
cntr <- list(
  contrast_a=data.frame(
    PrimaryID=annot$PrimaryID,
    padj=runif(6),
    pvalue=runif(6),
    log2FoldChange=rnorm(6),
    stringsAsFactors=FALSE
  )
)
covar <- data.frame(
  SampleID=colnames(exprs),
  group=c("A", "A", "B", "B"),
  stringsAsFactors=FALSE
)

if(interactive()) {
  ui <- fluidPage(heatmapUI("hm"))
  server <- function(input, output, session) {
    heatmapServer(
      "hm",
      annot=list(default=annot),
      exprs=list(default=exprs),
      cntr=list(default=cntr),
      covar=list(default=covar)
    )
  }
  shinyApp(ui, server)
}

if(interactive()) {
  data(C19)
  ui <- fluidPage(heatmapUI("hm"))
  server <- function(input, output, session) {
    heatmapServer(
      "hm",
      annot=list(default=C19$annotation),
      exprs=list(default=C19$expression),
      cntr=list(default=C19$contrasts),
      covar=list(default=C19$covariates),
      sample_id_col="label"
    )
  }
  shinyApp(ui, server)
}

}
