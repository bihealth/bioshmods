% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_gene_group_selector.R
\name{geneGroupSelectorUI}
\alias{geneGroupSelectorUI}
\alias{geneGroupSelectorServer}
\title{Shiny module for selecting groups of genes}
\usage{
geneGroupSelectorUI(id)

geneGroupSelectorServer(
  id,
  annot,
  exprs = NULL,
  cntr = NULL,
  mode_order = c("by_expression", "by_dge", "by_name"),
  primary_id = "PrimaryID",
  dge_pval_col = NULL,
  dge_lfc_col = NULL,
  dge_fdr_col = NULL,
  defaults = NULL,
  selected_ids = NULL,
  dataset = NULL
)
}
\arguments{
\item{id}{module identifier (same as passed to \code{\link[=geneGroupSelectorUI]{geneGroupSelectorUI()}})}

\item{annot}{annotation data frame (minimum input), or named list of data frames
for multiple datasets}

\item{exprs}{optional expression matrix/data frame, or named list of matrices/data
frames for multiple datasets}

\item{cntr}{optional contrasts list (named list of data frames), or named list of
such lists for multiple datasets}

\item{mode_order}{order of available selection modes. Values must come from
\code{c("by_expression", "by_dge", "by_name")}. Unavailable modes are skipped.
Default is expression -> DGE -> by name.}

\item{primary_id}{default annotation column used as the primary gene identifier}

\item{dge_pval_col}{optional p-value column name in contrast tables for DGE mode.
If \code{NULL}, the module auto-detects a suitable numeric column.}

\item{dge_lfc_col}{optional log fold-change column name in contrast tables for
DGE mode. If \code{NULL}, the module auto-detects a suitable numeric column.}

\item{dge_fdr_col}{optional adjusted p-value (FDR) column name in contrast tables
for DGE mode. If \code{NULL}, the module auto-detects a suitable numeric column.}

\item{defaults}{optional list of default selector parameters. Supported keys:
\code{expr_metric}, \code{expr_top_mode}, \code{expr_top_value}, \code{dge_top_mode},
\code{dge_top_value}, \code{dge_rank_mode}, \code{dge_pval_thr}, \code{dge_lfc_thr},
and \code{dge_require_fdr}.}

\item{selected_ids}{optional \code{reactiveVal()} that will be updated
with the currently selected PrimaryIDs (column \code{primary_id}) as a
character vector}

\item{dataset}{optional \code{reactiveVal()} that stores the currently selected
dataset name. If \code{NULL}, it is initialized internally with \code{reactiveVal()}.}
}
\value{
A list with reactives: \code{genes}, \code{annotation}, \code{dataset}, and \code{modus}.
}
\description{
The module supports three selection modi:
\enumerate{
\item by name (\code{data_type = by_name}) via free text and/or file upload;
\item by expression statistics (top variance or top average expression);
\item by differential expression results from contrast tables.
}
}
\details{
In DGE mode, users can choose ranking priority (\verb{lowest p-value first}
or \verb{highest absolute logFC first}) and optionally exclude rows with
missing adjusted p-values (FDR).
}
\examples{
## Minimal example
annot <- data.frame(
  PrimaryID=paste0("g", 1:5),
  SYMBOL=LETTERS[1:5]
)
exprs <- matrix(
  rnorm(20),
  nrow=5,
  dimnames=list(annot$PrimaryID, paste0("s", 1:4))
)
cntr <- list(
  contrast_a=data.frame(
    PrimaryID=annot$PrimaryID,
    padj=c(0.01, 0.20, 0.03, NA, 0.50),
    pvalue=c(0.001, 0.10, 0.02, 0.04, 0.80),
    log2FoldChange=c(2.1, -0.2, 1.3, 0.8, -3.2)
  )
)
if(interactive()) {
  ui <- fluidPage(
    sidebarLayout(
      sidebarPanel(geneGroupSelectorUI("gsel")),
      mainPanel(tableOutput("selected_ids"))
    )
  )
  server <- function(input, output, session) {
    selected_ids <- reactiveVal(character())

    geneGroupSelectorServer(
      "gsel",
      annot=annot,
      exprs=exprs,
      cntr=cntr,
      selected_ids=selected_ids
    )

    output$selected_ids <- renderTable({
      data.frame(PrimaryID=selected_ids(), stringsAsFactors=FALSE)
    })
  }
  shinyApp(ui, server)
}

## Example with C19 dataset
data(C19)
if(interactive()) {
  ui <- fluidPage(
    sidebarLayout(
      sidebarPanel(geneGroupSelectorUI("gsel")),
      mainPanel(tableOutput("selected_ids"))
    )
  )
  server <- function(input, output, session) {
    selected_ids <- reactiveVal(character())

    geneGroupSelectorServer(
      "gsel",
      annot=C19$annotation,
      exprs=C19$expression,
      cntr=C19$contrasts,
      selected_ids=selected_ids
    )

    output$selected_ids <- renderTable({
      data.frame(PrimaryID=selected_ids(), stringsAsFactors=FALSE)
    })
  }
  shinyApp(ui, server)
}
}
