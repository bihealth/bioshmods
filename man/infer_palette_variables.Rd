% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{infer_palette_variables}
\alias{infer_palette_variables}
\title{Infer Variable Specs for Color Palette Modules}
\usage{
infer_palette_variables(df, ordinal_n_levels = 10, cleanup = FALSE)
}
\arguments{
\item{df}{A data frame.}

\item{ordinal_n_levels}{Maximum number of unique values for integer columns
to be treated as ordinal. Default is \code{10}.}

\item{cleanup}{Logical; if \code{TRUE}, drops noisy variables:
\itemize{
\item categorical variables with all values unique,
\item categorical variables with more than 10 unique observed values,
\item any variable with exactly one unique non-missing value.
Default is \code{FALSE}.
}}
}
\value{
A named list where each item corresponds to one column of \code{df} and
contains:
\itemize{
\item discrete variables: \verb{list(type=<categorical|ordinal>, levels=<character>)}
\item continuous variables: \verb{list(type="continuous", breaks=<numeric length 2>)}
}
}
\description{
Convert a data frame into a variable specification list compatible with
\code{\link[=colorPalettesServer]{colorPalettesServer()}}, classifying each column as categorical, ordinal,
or continuous.
}
\details{
Classification rules:
\itemize{
\item Integer columns with fewer than \code{ordinal_n_levels} unique non-missing
values are treated as ordinal.
\item Other numeric columns are treated as continuous.
\item All other columns are treated as categorical.
}

For factor columns, levels are taken from \code{levels(x)}. For non-factor
categorical/ordinal columns, levels are taken from \code{unique(x)}.
Continuous variables receive \code{breaks} based on the numeric range.
}
\examples{
x <- data.frame(
  sex=factor(c("F", "M", "F"), levels=c("F", "M", "U")),
  stage=as.integer(c(1, 2, 3)),
  expr=c(10.1, 11.4, 9.8),
  cohort=c("A", "B", "A"),
  stringsAsFactors=FALSE
)

specs <- infer_palette_variables(x)
specs$sex
specs$stage
specs$expr
infer_palette_variables(x, cleanup=TRUE)

}
